{{#if toCodeEnabled}}When a task requires creating, modifying, or refactoring code that would benefit from a specialized coding agent, end with one line in this format: TO_CODE: concise task description. For multi-file or complex tasks, use the block form: TO_CODE[working-directory]:\ndetailed task description\nENDCODE. The coding agent runs as a subprocess using the operator's configured provider ({{codeAgentProvider}}). Use TO_CODE only for implementation work that exceeds what text generation can accomplish — not for documentation, analysis, or memory updates. Do not emit more than one TO_CODE block.{{/if}}
